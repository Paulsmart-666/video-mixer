# video-mixer — 开发规范

> 本文档约定 video-mixer 项目的强制开发规则、Git 流程与沟通规范。
> 风格参考个人其他仓库的「项目规范」，但已针对本项目的实际领域（本地运行的视频批量混剪工具：FastAPI 后端 + React/Vite 前端 + ffmpeg 合成）做适配。

---

## 🔴 铁律（每次必执行，不需要用户提醒）

### 1. 更新日志强制更新 + 版本号同步
- **任何 `backend/` 或 `frontend/` 内的代码改动都必须更新 `更新日志.md`**，不遗漏。**包括小改动**（参数调整、逻辑修正、样式微调等），无论改动大小都必须在 `更新日志.md` 中记录。
- 同一功能的多次迭代修改也要分别记录，清晰追溯演进过程。
- 按「新增 / 优化 / 修复 / 规范」四类归类。
- 版本标题格式见下方「更新日志标题格式规范（强制）」：当前版本 `## [版本号]（当前版本）{emoji} 标题`，历史版本 `## [版本号] {emoji} 标题`；`]` 与 emoji/标签之间**有且只有一个空格**；不写日期。
- **版本号真相源 = `更新日志.md` 顶部「当前版本」条目**。发版时同步更新 `frontend/package.json` 的 `version` 字段，并打 Git tag `vX.Y.Z`。`更新日志.md` 版本号、`package.json` 的 `version`、Git tag 三者必须一致，发版前逐项核对。
- **文档类编辑不推版本号**：仅改动文档（`.md` 文件：README / 更新日志 / 本规范等），**不含 `backend/` `frontend/src/` 内任何代码逻辑变更**时，**不递增版本号、不打 Git tag、不新增版本条目**。此类改动直接 `git add -A && git commit && git push` 即可，无需走「版本号 + 更新日志版本条目 + tag」三件套。版本号仅随 `backend/` `frontend/` 内的代码功能 / 修复变更而递增。
- **md 文档随开发全程同步（开发中 + 完成后都不遗漏）**：除 `更新日志.md` 外，README 等文档也必须在开发过程中持续保持同步，不能等全部做完才一次性补写。

#### 更新日志标题格式规范（强制）
- **全文档只允许一个 `（当前版本）`**：格式 `## [最新版本号]（当前版本）{emoji} 标题`，且必须位于文件**最顶部**（第一个版本条目）。`]` **直接接** `（当前版本）`（无空格），emoji **直接接** `（当前版本）`（无空格）。
- **每次新增版本条目时，必须先移除上一个 `## [x.y.z]（当前版本）` 的 `（当前版本）` 后缀**，改为普通标题 `## [x.y.z] {emoji} 标题`。这一步与"新增条目"同等重要，缺一不可。发版前必须用 `grep -c '（当前版本）' 更新日志.md` 核验结果恰为 1；若 >1 说明有历史版本漏去后缀。
  - **降级补空格**：当前版本条目格式为 `## [x.y.z]（当前版本）{emoji} 标题`（`]` 后直接接 `（当前版本）`，无空格）。去掉 `（当前版本）` 后，`]` 会直接紧挨 emoji 变成 `]🔧` 无空格写法——**必须同步在 `]` 后补一个空格**，使其符合历史标题 `## [x.y.z] {emoji} 标题` 格式。
- **历史版本标题统一为 `## [x.y.z] {emoji} 标题`**（不带 `（当前版本）` 后缀）。
- **`]` 与 emoji/标签之间必须且仅有「一个空格」**：全文档统一，禁止 `]🔧` / `]🎯` 这类无空格写法。当前版本条目例外：`]` 后直接接 `（当前版本）`，无空格。
- **版本标题（H2）必须独占一行**：`###` 小节必须另起一行，严禁把 `### 小节` 或正文粘进 `##` 标题同一行。
- 条目分类沿用「新增 / 优化 / 修复 / 规范」，格式基于 Keep a Changelog。

#### 更新日志格式自检（发版 / 纯文档提交前必跑）
> 每次提交 `更新日志.md`（含新增版本条目、纯格式修正）前，用以下命令核验格式。任一条有意外输出即代表格式错误，必须修掉再 push。

- **仅一个当前版本标记**（且为文件首行 `##`）：
  `grep -c '（当前版本）' 更新日志.md` → 必须输出 `1`
- **非当前版本标题 `]` 后必有空格**（无空格即违规）：
  `grep -nE '^## \[[0-9.]+\][^ （]' 更新日志.md | grep -v '（当前版本）'` → 必须**无输出**
- **标题行不得混入 `###`**（标题必须独占一行）：
  `grep -nE '^## .*###' 更新日志.md` → 必须**无输出**

#### 版本号递增规则
| 字段 | 含义 | 何时递增 |
|------|------|---------|
| **主版本** `X.0` | 大版本，里程碑 | 破坏性重构 / 技术栈升级 / 整体架构变更 |
| **次版本** `X.Y` | 功能更新 | 新增玩法 / 新增功能模块 / 新增导出路径 |
| **修订号** `X.Y.Z` | Bug 修复 / 小优化 | 修复问题 / 参数调优 / UI 微调 |

#### 版本号更新检查清单（代码变更推送前执行；纯文档编辑跳过以下版本 / tag 项）
- [ ] `更新日志.md` 已添加对应条目（新增/优化/修复/规范）
- [ ] `更新日志.md` 中**仅一个** `（当前版本）` 标记（发版时已把上一个的后缀去掉）
- [ ] `更新日志.md` 格式自检通过（见上方）：`grep -c '（当前版本）'` = 1；非当前版本标题 `]` 后有空格；标题行无混入 `###`
- [ ] `frontend/package.json` 的 `version` 已与 `更新日志.md` 当前版本号同步
- [ ] Git tag 已创建并推送（`git tag -a vX.Y.Z -m "vX.Y.Z"` + `git push origin vX.Y.Z`）
- [ ] `更新日志.md` 版本号、`package.json` version、Git tag 三者一致

### 2. Git 配置（固定，不可变）
```bash
git config user.email "baoluopaulsmart@gmail.com"
git config user.name "Paulsmart-666"
```

### 3. 构建验证
- **前端**：`pnpm install && pnpm build` 通过才能 push（构建产物落 `frontend/dist`，由后端统一托管）。
- **后端**：`python3 -c "import app.main"` 无导入错误、依赖（见 `backend/requirements.txt`）已安装，方可 push。
- 任一构建 / 导入失败，先本地修好再提交。

### 4. 素材与产出文件安全
- **素材库扫描只读不写**：`scanner` 只读取用户素材目录结构与时长，严禁对源素材执行删除、重命名、移动或覆盖。
- **合成不破坏源**：成片只写入用户指定的导出目录；禁止把导出目录默认指向素材根目录或任何源路径。
- **组合算法不得污染输入**：去重 / 拒绝采样等逻辑只作用于输出编排，不修改源文件。

---

## 项目开发流程

### 同步机制
本项目为本地运行工具，仅使用 **GitHub** 同步（无自动部署平台）：

1. 每次修改完成后：
   - `git add -A && git commit -m "描述" && git push origin main`
2. push 后团队成员 / 其他设备 `git pull` 即可获取最新代码。

### 版本管理
- `git log --oneline` 查看历史
- `git reset --hard <commit>` 回退
- `git tag -a vX.Y.Z -m "vX.Y.Z"` 打版本标签，`git push origin vX.Y.Z` 推送标签
- 用户说"回退"直接执行，不用确认

### 开发偏好
- 修改前先 `Read` 当前文件内容
- 小步提交，每次只改一个方面
- 不要擅自做大范围重构
- 用户可能发截图，看不到就请文字描述

---

## 沟通规范

### 回复要求
- 用简体中文
- 先理解问题再动手，不要急于写代码
- 修改后必须 `git push` 到 `main`
- 每次提交要写清楚的 commit message
- **每次代码改动都要更新 `更新日志.md`**：任何新增功能、优化、修复（即 `backend/` `frontend/` 内的代码变更）都必须追加到对应版本下，按「新增 / 优化 / 修复 / 规范」四类归类，不遗漏任何修改。**这是强制规则，不需要用户提醒。**（纯 `.md` 文档编辑按「文档类编辑不推版本号」规则处理，不新增版本条目、不递增版本号。）
- `更新日志.md` 格式：当前版本 `## [版本号]（当前版本）{emoji} 标题`、历史版本 `## [版本号] {emoji} 标题`；`]` 与 emoji 之间**有且只有一个空格**；不写日期；新版本发布时把旧版的「（当前版本）」去掉，全文档仅保留一个。提交前跑「更新日志格式自检」三条 grep 命令，格式错误必须修掉再 push。

### 常见问题类型与处理方式

1. **ffmpeg 编码失败 / libopenh264 缺失**：检查后端是否安装带 libopenh264 的 ffmpeg；无 GPU 时应自动走软件编码路径。
2. **组合去重异常（出现重复成片）**：检查 `combinator` / `composer` 的穷举去重与拒绝采样逻辑边界。
3. **成片超时长上限**：检查时长限制分支（30秒~3分钟 / 不限）是否生效，组合阶段是否预筛超长片段。
4. **素材目录分类识别错误**：检查 `scanner` 对一级子文件夹（前贴 / 口播开头 / 口播中间 / 口播结尾）的识别与未分类提示。
5. **前后端代理 / 跨域**：检查前端 dev 代理配置与后端 CORS / 端口（默认 8000）是否一致。
6. **产出任务卡住 / 无下载链接**：检查 `task_manager` 的线程池调度、进度回调与文件落盘路径。
