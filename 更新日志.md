# Changelog

本文档记录 video-mixer 的所有重要变更。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)。

---

## [1.1.6]（当前版本）🐛 修复成片下载接口 500 错误

### 修复
- **成片下载 500**：`/api/files/download` 原使用 `FileResponse`（分块流式响应），在 CloudStudio 网关下对中文文件名 `Content-Disposition` 头处理失败，对所有成片返回 `500 Internal Server Error`，前端无法取回合成结果。现改为小文件（≤200MB）一次性读入内存返回普通 `Response`（非流式），并按 RFC 5987 显式编码中文文件名（`filename*=UTF-8''`）；超大文件仍回退 `FileResponse`。修复后经端到端验证：下载接口 `HTTP 200`，可正常取回 23.5KB / 15.8MB 成片，ffprobe 确认 mp4 结构完整、时长正常。

### 规范
- 版本号：`1.1.6`（package.json / 更新日志 / git tag 一致）。

## [1.1.5] 🔧 ffmpeg/ffprobe 解析收口，去掉手动拷贝依赖

### 修复
- **ffmpeg 解析收口**：新增 `backend/app/services/ffmpeg_path.py`，集中提供 `get_ffmpeg_exe()` / `get_ffprobe_exe()`。`encoder_profile`、`ffmpeg_builder`、`settings`、`probe` 全部改为从此处解析 ffmpeg/ffprobe，不再依赖 `shutil.which("ffmpeg"/"ffprobe")` 或字面 `"ffmpeg"`。
- **去掉手动拷贝依赖**：`get_ffmpeg_exe()` 优先使用 PATH 中的 ffmpeg（系统或 static_ffmpeg 提供的、支持硬件编码 nvenc 的版本），找不到时退回 `imageio_ffmpeg` 自带的静态 ffmpeg（含 libx264，必随包安装存在）。不再要求把 ffmpeg 手动拷贝进 `.venv/Scripts` 才能运行；venv 重置或打包 EXE 后只要依赖在，即可定位到可用 ffmpeg。

### 规范
- 版本号：`1.1.5`（package.json / 更新日志 / git tag 一致）。

## [1.1.4] 🐛 修复素材库「清空/批量删除」报 500 错误

### 修复
- **批量删除 500**：后端 `delete_clip` / `delete_clips` 原直接用 `os.remove`，在 WorkBuddy 沙箱等安全策略拦截删除的环境中会抛 `SAFE_DELETE_FAIL_CLOSED`，返回 500，前端显示「内部/Server Error」。现增加 `_safe_remove`：先尝试 `os.remove`，被拦截时通过 `shutil.move` 把文件移到该分类目录内的 `.trash/` 隔离，达到清空效果。
- **扫描忽略回收隔离目录**：`scanner.py` 的 `IGNORE_DIRS` 增加 `.trash`，避免被移到隔离区的文件继续出现在素材库。
- **删除结果更友好**：批量删除接口返回 `failed` 明细，前端 `MaterialLibraryPanel` 在有失败项时显示具体文件名；`deleteClips` 类型补齐 `failed` 字段。

### 规范
- 版本号：`1.1.4`（package.json / 更新日志 / git tag 一致）。

## [1.1.3] 🐛 还原素材库原始结构并确保目录切换后自动刷新

### 修复
- **还原素材库面板结构**：将 `MaterialLibraryPanel` 还原为初始结构，仅展示分类卡片列表，顶部不再嵌入「选择素材目录」按钮，避免破坏原有布局。
- **目录切换自动刷新**：目录选择入口保留在顶部 `TopBar`，并在切换素材目录后自动 `refreshLibrary()`，避免切完目录还要手动刷新才能看到分类。

### 规范
- 版本号：`1.1.3`（package.json / 更新日志 / git tag 一致）。

## [1.1.2] 🐛 修复 ffprobe 缺失导致 ffmpeg_ok 误报

### 修复
- **ffprobe 未进入 PATH**：`run.py` 仅把 `.venv/Scripts` 加进 PATH，而 `ffprobe` 只随 `static_ffmpeg` 落在 site-packages 的 bin 目录，导致 `_check_ffmpeg()` 找不到 ffprobe、`ffmpeg_ok` 误报 false，进而 `probe.py` 扫描素材时长失败。现 `run.py` 额外把 `static_ffmpeg` 的 bin 目录加入 PATH，开发态与打包 EXE 均可在 PATH 内同时定位 ffmpeg/ffprobe。
- **默认素材目录缺失**：重建 `materials/` 下 `前贴 / 口播开头 / 口播中间 / 口播结尾` 四个空分类文件夹（数据目录，gitignore），避免首次打开即报「素材目录不存在」。

## [1.1.1] 🐛 素材库空状态增加「选择素材目录」入口

### 修复
- **素材库导入入口不可见**：当素材根目录未设置、不存在或缺少分类时，分类卡片不渲染，导致原本藏在分类卡片里的「导入」按钮无法显示。现已在素材库面板顶部常驻「选择素材目录」按钮，并在空状态给出显式引导，选中目录后自动刷新素材库。

## [1.1.0] ✨ 导出目录生效 + 水印/BGM 可用

### 新增
- **导出文件夹真正生效**：原「选择本机文件夹」仅记录文件夹名、成片仍写到后端运行目录。改为通过后端目录浏览选择并持久化到 `settings.output_dir`，合成时成片写入所选目录；目录浏览的「上级」改用服务端返回路径，修复 Windows 反斜杠路径错误。
- **水印文字输入**：开启「水印」后展开文字输入框，合成的成片带水印；水印字体按平台选择（Windows 用 `msyh.ttc` 等），找不到字体时退回 ffmpeg 默认字体，避免 Windows 下报错。
- **BGM 上传**：新增 `POST /api/files/upload-bgm`，开启「BGM」后可上传音频文件，合成的成片混入背景音。

### 修复
- **设置持久化**：前端 `updateSettings` 此前只更新本地状态、不调用后端，导致素材目录/导出目录切换刷新后失效；现改为真正写入后端 `settings.json`。
- **探测缓存路径**：`scanner` 默认缓存路径由硬编码 `/workspace/...` 改为基于项目根目录，消除 Linux 路径假定。

### 规范
- README 合成路径说明更正为「统一走 filter_complex 归一化重编码」，并补充说明字幕为占位、水印/BGM 需填写/上传后生效。

## [1.0.5] 🐛 修复错误响应解析 + 清空测试素材 + 版本显示正确

### 修复
- **前端错误响应解析**：`frontend/src/api/client.ts` 在 HTTP 错误时先 `res.text()` 再 `JSON.parse()`，避免 `res.json()` 失败后再次读 `res.text()` 报 `body stream already read`。
- **FormData 上传请求头**：上传素材时不再强制设置 `Content-Type: application/json`，让浏览器自动带 `multipart/form-data` 边界。
- **版本号显示**：重新构建前端 `dist`，使顶栏版本号与 `package.json` 一致为 `v1.0.5`。

### 变更
- **清空测试素材**：删除 `materials/` 下用于本地验证的 9 段测试视频，保留空分类文件夹，首次打开素材库为空白状态。

## [1.0.4] 🐛 补充一键启动与本地访问说明

### 新增
- **一键启动脚本**：项目根目录新增 `start.bat`，Windows 下双击即可启动后端服务并自动打开浏览器访问 `http://localhost:8000`。

### 变更
- **README 访问说明置顶**：在 README 顶部新增「快速开始」区块，强调「必须先启动后端，再访问 `http://localhost:8000`」，避免直接打开本地地址时报错。

## [1.0.3] 🐛 本地路径适配 + 明确 EXE 化方向

### 变更
- **项目方向调整**：README 移除公网部署与 Oracle Cloud 常驻部署内容，明确项目为「本地桌面工具」，最终目标为打包成 Windows 单机 EXE。
- **README 重写**：「在线体验与部署」改为「项目方向」+「本地运行方式」+「EXE 化方向」；运行说明改用 `npm install` / `npm run build` + venv，贴合本地开发。

### 修复
- **后端 dist 路径**：`backend/app/main.py` 的 `dist_path` 从硬编码 `/workspace/frontend/dist` 改为基于 `__file__` 的相对路径，本地运行可正确托管前端。
- **默认目录本地化**：`backend/app/config.py` 的 `material_root`、`output_dir`、`settings.json` 从 `/workspace/...` 改为相对项目根目录的 `materials/`、`output/`、`backend/data/settings.json`，本地和 EXE 打包通用。
- **ffmpeg 路径自动注入**：`backend/run.py` 启动时自动将当前 Python 解释器所在目录加入 PATH，venv 与 EXE 场景无需手动配置 ffmpeg/ffprobe 路径。

## [1.0.2] ✨ 工具界面显示版本号

### 新增
- **UI 版本号**：顶栏标题旁显示当前版本（如 `v1.0.2`），方便核对线上/本地构建对应的更新日志条目。
- **构建期注入**：`vite.config.ts` 通过 `define` 将 `package.json` 的 `version` 注入为全局 `__APP_VERSION__`；新增 `src/version.ts` 与其类型声明 `src/vite-env.d.ts`，版本号始终与「当前版本」标记、Git tag `v1.0.2` 保持一致。

## [1.0.1] 🐛 补全缺失前端模块，工具可构建运行

### 新增
- **前端类型与常量** `src/types/index.ts`：与后端 models 对齐的 TS 类型（素材/分类/组合请求/批次/任务/进度/设置等），以及 `FRAMEWORK_SEQUENCE` / `FRAMEWORK_LABELS` / `DURATION_OPTIONS` 框架常量。
- **API 客户端** `src/api/client.ts`：封装后端全部端点（`library` 扫描/刷新/上传/删除、`compose` 预览/提交/批次/取消、`files` 浏览/下载、`settings` 读写）。
- **全局状态** `src/store/useAppStore.ts`（zustand）：集中管理素材库、合成配置、批次任务与设置，含合成批次轮询逻辑。
- **构建配置** `vite.config.ts`：`@` 别名指向 `src`，并配置 dev 代理将 `/api` 转发到后端 8000。

### 修复
- 补全后 `pnpm build`（tsc + vite）通过，工具首次可完整构建运行。
- 修正 `ComposePanel` 对 `OutputDirPicker` 的引用路径（`../common/` → `./`）。

## [1.0.0] 🎉 初始版本：素材库 + 爆款批量合成

### Added
- **素材库**：指定本地素材根目录，自动识别一级子文件夹分类（`前贴` / `口播开头` / `口播中间` / `口播结尾`），列出每个分类下的视频素材及时长，自动识别并提示未分类文件夹。
- **爆款合成**：
  - 框架1（4 段）：前贴 + 口播开头 + 口播中间 + 口播结尾
  - 框架2（3 段）：口播开头 + 口播中间 + 口播结尾
  - 产出数量（理论最大组合数实时显示）
  - 成片时长限制（30 秒 ~ 3 分钟，或不限）
  - 文件命名规则，支持占位符 `{n}` `{framework}` `{pre}` `{opening}` `{middle}` `{ending}` `{duration}`
  - 导出文件夹选择
  - 合成选项开关：转场 / 字幕 / 水印 / BGM / 人声统一
- **产出进度**：批次任务列表实时轮询，显示每个任务的组成片段、进度、下载链接，支持停止批次。
- **后端**：FastAPI 服务 + ffmpeg（libopenh264 软件编码，无 GPU 环境自动适配），线程池并发合成；组合算法（小组合空间穷举去重、大空间拒绝采样，保证不重复且不超过时长上限）；合成路径（编码参数一致走 `concat -c copy` 快速路径，否则走 `filter_complex` 统一画布后编码）。
- **前端**：Vite + React + Tailwind + zustand；开发模式独立运行（5173），生产模式构建到 `frontend/dist` 后由后端在 8000 端口统一托管。
- **运行脚本**：`start.sh` 一键启动前后端。
