# Changelog

本文档记录 video-mixer 的所有重要变更。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)。

---

## [1.0.2]（当前版本）✨ 工具界面显示版本号

### 新增
- **UI 版本号**：顶栏标题旁显示当前版本（如 `v1.0.2`），方便核对线上/本地构建对应的更新日志条目。
- **构建期注入**：`vite.config.ts` 通过 `define` 将 `package.json` 的 `version` 注入为全局 `__APP_VERSION__`；新增 `src/version.ts` 与其类型声明 `src/vite-env.d.ts`，版本号始终与「当前版本」标记、Git tag `v1.0.2` 保持一致。

## [1.0.1] 🐛 补全缺失前端模块，工具可构建运行

### 新增
- **前端类型与常量** `src/types/index.ts`：与后端 models 对齐的 TS 类型（素材/分类/组合请求/批次/任务/进度/设置等），以及 `FRAMEWORK_SEQUENCE` / `FRAMEWORK_LABELS` / `DURATION_OPTIONS` 框架常量。
- **API 客户端** `src/api/client.ts`：封装后端全部端点（`library` 扫描/刷新/上传/删除、`compose` 预览/提交/批次/取消、`files` 浏览/下载、`settings` 读写）。
- **全局状态** `src/store/useAppStore.ts`（zustand）：集中管理素材库、合成配置、批次任务与设置，含合成批次轮询逻辑。
- **构建配置** `vite.config.ts`：`@` 别名指向 `src`，并配置 dev 代理将 `/api` 转发到后端 8000。

### 修复
- 补全后 `pnpm build`（tsc + vite）通过，工具首次可完整构建运行。
- 修正 `ComposePanel` 对 `OutputDirPicker` 的引用路径（`../common/` → `./`）。

## [1.0.0] 🎉 初始版本：素材库 + 爆款批量合成

### Added
- **素材库**：指定本地素材根目录，自动识别一级子文件夹分类（`前贴` / `口播开头` / `口播中间` / `口播结尾`），列出每个分类下的视频素材及时长，自动识别并提示未分类文件夹。
- **爆款合成**：
  - 框架1（4 段）：前贴 + 口播开头 + 口播中间 + 口播结尾
  - 框架2（3 段）：口播开头 + 口播中间 + 口播结尾
  - 产出数量（理论最大组合数实时显示）
  - 成片时长限制（30 秒 ~ 3 分钟，或不限）
  - 文件命名规则，支持占位符 `{n}` `{framework}` `{pre}` `{opening}` `{middle}` `{ending}` `{duration}`
  - 导出文件夹选择
  - 合成选项开关：转场 / 字幕 / 水印 / BGM / 人声统一
- **产出进度**：批次任务列表实时轮询，显示每个任务的组成片段、进度、下载链接，支持停止批次。
- **后端**：FastAPI 服务 + ffmpeg（libopenh264 软件编码，无 GPU 环境自动适配），线程池并发合成；组合算法（小组合空间穷举去重、大空间拒绝采样，保证不重复且不超过时长上限）；合成路径（编码参数一致走 `concat -c copy` 快速路径，否则走 `filter_complex` 统一画布后编码）。
- **前端**：Vite + React + Tailwind + zustand；开发模式独立运行（5173），生产模式构建到 `frontend/dist` 后由后端在 8000 端口统一托管。
- **运行脚本**：`start.sh` 一键启动前后端。
