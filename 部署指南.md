# video-mixer 部署指南（容器化 · 免费常驻）

本工具是**本地运行的视频批量混剪工具**，依赖常驻进程 + ffmpeg + 持久磁盘，因此**不适合 Vercel 这类 Serverless 平台**（详见 README「关于部署平台」）。本指南给出基于仓库根目录 `Dockerfile` 的容器化部署方案，目标是在**免费、常驻不休眠**的平台上长期运行。

## 一、镜像说明

`Dockerfile` 为**多阶段构建**：

| 阶段 | 基础镜像 | 作用 |
|------|---------|------|
| 构建前端 | `node:20-slim` | `pnpm install` + `pnpm build` 产出 `frontend/dist` |
| 运行环境 | `ubuntu:22.04`（系统 Python 3.10）+ ffmpeg | 安装后端依赖，uvicorn 监听 **8000** |

关键点：

- **ffmpeg 编码器自动探测**：程序启动时会检测可用的 H.264 编码器，优先 `libopenh264`，无则用 `libx264` 回退（见 `backend/app/services/encoder_profile.py`）。Ubuntu 22.04 的 `apt` 版 ffmpeg 自带 `libopenh264`，与沙箱环境一致。
- **前端托管**：`main.py` 在 `/workspace/frontend/dist` 挂载静态文件，由后端在 8000 端口统一托管（生产模式单端口）。
- **需持久化的目录**（重部署不丢数据，必须用卷挂载）：
  - `/workspace/materials` —— 素材根目录
  - `/workspace/output` —— 成片导出目录
  - `/workspace/backend/data` —— 设置文件 `settings.json`（可选，不挂则每次重部署恢复默认）

## 二、部署到 Oracle Cloud Always Free（推荐 · 永久免费常驻）

> **2026-08 重要更新**：此前推荐的 **ClawCloud Run 已于 2026 年 5 月 11 日停服**，ClawCloud VPS/VDS 也于 **2026 年 6 月 30 日全面关停**，无法再使用。当前免费常驻首选已替换为 Oracle Cloud Always Free。

目前**真正「永久免费 + 常驻不睡」**的方案首推 [Oracle Cloud Always Free](https://www.oracle.com/cn/cloud/free/)：

- 免费配额：2× AMD VM（1G） 或 4× ARM VM（4核24G） + 200G 块存储 + 每月 10TB 出站流量
- 无到期时间、不休眠
- 代价：需**信用卡**验证；无国内节点（最近东京/首尔/新加坡/孟买）；注册偶有风控

部署步骤（以 Ubuntu 22.04 ARM 实例为例）：

1. 注册 Oracle Cloud 并创建 Always Free 实例（选择 **Ubuntu 22.04** 镜像；建议 ARM 4核24G，保底也可选 AMD 1G）。
2. SSH 登录实例，安装 Docker：
   ```bash
   sudo apt-get update
   sudo apt-get install -y docker.io git
   sudo usermod -aG docker $USER
   newgrp docker
   ```
3. 克隆仓库并构建镜像：
   ```bash
   git clone https://github.com/Paulsmart-666/video-mixer.git
   cd video-mixer
   docker build -t video-mixer .
   ```
4. 创建本地持久目录并运行容器（用 `-v` 把宿主机目录挂进容器，重部署/重启不丢数据）：
   ```bash
   mkdir -p materials output backend/data
   docker run -d --name video-mixer \
     -p 8000:8000 \
     -v "$(pwd)/materials:/workspace/materials" \
     -v "$(pwd)/output:/workspace/output" \
     -v "$(pwd)/backend/data:/workspace/backend/data" \
     --restart unless-stopped \
     video-mixer
   ```
5. 在 Oracle Cloud 控制台**安全组 / VCN 防火墙**里放行入站 **TCP 8000**。
6. 浏览器访问 `http://<实例公网IP>:8000`，即可使用。

> 生产访问建议加一层 HTTPS（Caddy / Nginx 反向代理，或 Cloudflare Tunnel），避免直接暴露 8000。

## 三、备选免费 / 低价平台

| 平台 | 特点 | 注意 |
|------|------|------|
| **雨云 (RainYun)** | 国内便宜 VPS，~30元/月起，延迟低、支付宝付款 | **不免费**，但成本最低；需自行安装 Docker 并按第二节步骤部署 |
| **Render** | GUI 最省心、GitHub 自动部署、自带持久磁盘 | **免费档会休眠**（15 分钟无访问）；常驻需 Starter 约 $7/月 |
| **Railway** | 用量计费、DX 顺手、易挂卷 | 无稳定免费档，约 $5/月用量下限 |
| **阿里云/腾讯云/华为云 轻量** | 国内节点、新用户首年很便宜 | 新用户有短期免费/低价，续费变贵；需实名 |

## 四、验证部署

容器启动后，访问 `https://<你的域名>/api/settings`，应返回类似：

```json
{
  "material_root": "/workspace/materials",
  "output_dir": "/workspace/output",
  "concurrency": 2,
  "encoder_name": "libopenh264",
  "ffmpeg_ok": true
}
```

`ffmpeg_ok: true` 即表示编码器可用，可以开始合成。

## 五、故障排查

- **`ffmpeg_ok: false` / 启动报"未找到可用的 H.264 编码器"**：镜像未正确安装 ffmpeg。确认 `Dockerfile` 中 `apt-get install ffmpeg` 步骤成功；若切换到 Debian 系基础镜像（如 `python:3.11-slim`），其 `apt` ffmpeg 不含 `libopenh264`，程序会自动回退到 `libx264`，仍可正常工作。
- **素材 / 成片重部署后消失**：未挂载持久卷。按第二节第 5 步挂载 `materials`、`output`、`backend/data`。
- **改了代码看不到更新**：重新触发构建 / 重新部署，并确认是从 `main` 分支拉取。
- **并发 OOM**：在小内存档把 `backend/app/config.py` 的 `concurrency` 调到 `1`（免费档 8G 通常无需改动）。
