# 新窗口续开发话术（video-mixer）

## 一句话
开新窗口只要说 **【继续】**，我就知道是接着 video-mixer 干：读一下项目记忆（`.workbuddy/memory`）和 `video-mixer-repo` 现状，从断点继续。

## 你说什么 → 我做什么
| 你说 | 我做什么 |
|------|----------|
| **【继续】** | 读记忆 + 仓库状态，回到上次进度接着干 |
| **【继续：<具体任务>】** | 同上，但明确这次要做的那件事 |
| **【恢复】** | 仅本机仓库丢了 / 换机器时：读共享盘 → 从 vX.Y.Z bundle 还原 → 设 origin 为 GitHub → 回到【继续】 |

## 不用说的
- 不用每次说"读共享盘"——那是**恢复专用**，平时不说。
- 不用重复项目背景——记忆里有。

## 铁律（每次改动都守）
改代码必同步 `更新日志.md` + `package.json` 的 `version` + Git tag（三者一致）；git 身份固定；推送前 `pnpm build` 通过；token 临时给、不落盘。
